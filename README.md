

```
cargo run


Output device: MacBook Pro Speakers
Default output config: SupportedStreamConfig { channels: 2, sample_rate: SampleRate(44100), buffer_size: Range { min: 14, max: 4096 }, sample_format: F32 }

Drum synthesizer ready!
Commands:
  k - trigger kick
  q - quit

Kick parameters:
  Start frequency: 80 Hz
  End frequency: 45 Hz
  Pitch decay: 0.08s
  Click level: 0.4
  Noise level: 0.3
  Noise filter cutoff: 2000 Hz
  Noise decay: 0.05s

```

input "k" to trigger kick audio sound
